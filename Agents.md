# Agents.md - Google Jules を活用した開発プロセス

このドキュメントでは、Google Jules を用いた開発タスクの実装プロセスについて説明します。Jules を最大限に活用し、効率的かつ高品質な開発を実現するためのガイドラインを提供します。

## 1. 開発プロセスのフェーズ

Jules と連携して開発を進める際の主要なフェーズは以下の通りです。

### 1.1. タスクの理解 (Understanding the Task)

*   **要件定義の確認:** 開発タスクの目的、機能要件、非機能要件を明確に理解します。関連する `requirements.md` や `DevDoc_Req-Fnc-001.md` などのドキュメントを参照します。
*   **既存コードベースの分析:** 変更対象となる既存のコード、関連するモジュール、依存関係をJulesに提示し、その構造とロジックを理解させます。`read_file` や `read_many_files` ツールを使用して、必要なファイルの内容をJulesに提供します。
*   **関連ドキュメントの参照:** 設計ドキュメント、API仕様、テスト計画など、タスクに関連するあらゆる情報をJulesに提供し、包括的な理解を促します。

### 1.2. 計画 (Planning)

*   **実装方針の策定:** Jules と対話しながら、タスクを達成するための具体的な実装アプローチ、使用する技術スタック、主要なコンポーネントなどを検討します。
*   **テスト戦略の検討 (TDD - t_wada方式):** E2Eテスト仕様書からテストケースを段階的に詳細化し、それに基づいてユニットテストを設計します。Jules には、この詳細化プロセスを支援させ、適切なユニットテストの作成を依頼します。
*   **影響範囲の特定:** 変更がシステム全体に与える影響をJulesと共に分析し、潜在的なリスクや副作用を特定します。

### 1.3. 実装 (Implementation)

*   **TDDサイクル (Red-Green-Refactor):**
    *   **Red:** まず、作成したユニットテストが失敗することを確認します。Jules には、この失敗するテストケースの作成を支援させます。
    *   **Green:** 次に、そのユニットテストが成功する最小限のコードを実装します。Jules には、この最小限のコード実装を支援させます。
    *   **Refactor:** ユニットテストがすべて成功した状態（Green）で、コードの品質（可読性、保守性、パフォーマンスなど）を向上させるためのリファクタリングを行います。Jules には、このリファクタリング作業を支援させます。
*   **コード生成と修正:** Jules に具体的なコードの生成や既存コードの修正を依頼します。この際、プロジェクトのコーディング規約、スタイル、既存のパターンに沿うように明確な指示を与えます。`write_file` や `replace` ツールを使用します。
*   **既存コードとの統合:** 生成または修正されたコードが、既存のコードベースとシームレスに統合されるように、Jules に指示します。
*   **ベストプラクティスの遵守:** セキュリティ、パフォーマンス、保守性などの観点から、業界のベストプラクティスやプロジェクト固有のガイドラインを遵守するようJulesに求めます。

### 1.4. 検証 (Verification)

*   **単体テストの実行:** Jules が生成または修正したコードに対して、単体テストを実行し、期待通りに動作することを確認します。`run_shell_command` を使用してテストコマンドを実行します。
*   **リンティングと静的解析:** プロジェクトで定義されたリンティングルールや静的解析ツールを用いて、コード品質とスタイルの一貫性をチェックします。
*   **プルリクエストの作成:** ユニットテストがすべてグリーンになり、リファクタリングが完了したら、変更内容をプルリクエストとして提出します。Jules には、プルリクエストの準備（コミットメッセージの作成など）を支援させます。
*   **手動E2Eテストの実施:** プルリクエストがマージされる前に、人間がE2Eテスト仕様書に基づいた手動テストを実施し、意図した振る舞いが実現されているかを確認します。

### 1.5. フィードバックと改善 (Feedback and Improvement)

*   **Julesへのフィードバック:** Jules が生成したコードや提案に対して、具体的なフィードバックを提供し、次回の生成や修正の精度向上に役立てます。
*   **コードレビューと修正:** Jules が作成したコードは、最終的に人間の開発者によるレビューを受け、必要に応じて修正を加えます。

## 2. Jules とのインタラクションガイドライン

Jules との効率的な対話のために、以下の点に留意してください。

*   **明確な指示 (Clear Instructions):** タスクの目的、期待される出力、制約条件などを具体的に、かつ曖昧さなく指示します。
*   **段階的なアプローチ (Step-by-step Approach):** 複雑なタスクは、小さなサブタスクに分割し、段階的にJulesに依頼することで、より正確な結果を得やすくなります。
*   **コンテキストの提供 (Providing Context):** 関連するファイルの内容、エラーメッセージ、既存のコードスニペットなど、Jules がタスクを理解するために必要なすべてのコンテキストを提供します。
*   **検証と確認 (Verification and Confirmation):** Jules が提案した変更や生成したコードは、必ず人間が確認し、必要に応じて修正を指示します。
*   **エラーハンドリング (Error Handling):** エラーが発生した場合は、エラーメッセージと関連するコードをJulesに提示し、デバッグと修正を依頼します。

## 3. 考慮事項

*   **セキュリティ (Security):** Jules が生成するコードには、潜在的なセキュリティ脆弱性が含まれる可能性があります。常にセキュリティレビューを実施し、安全なコードであることを確認してください。
*   **パフォーマンス (Performance):** 生成されたコードのパフォーマンスを評価し、必要に応じて最適化を検討します。
*   **保守性 (Maintainability):** 将来的な保守性を考慮し、可読性が高く、理解しやすいコードであることを確認します。
*   **ドキュメンテーション (Documentation):** Jules が関与した開発タスクについても、適切なドキュメンテーション（設計書、コメントなど）を作成します。